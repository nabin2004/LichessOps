Feast feature repository for the Lichess project.

This repo is mounted into the Feast feature server container. Add your
feature definitions in feature_defs.py and run `feast apply` via the
feast-cli service to update the registry.
