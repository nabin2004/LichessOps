# Object storage and DuckDB (deprecated)

This document has been replaced by [ColumnStore analytics](./columnstore-analytics.md).

DuckDB was removed in favor of **MariaDB ColumnStore** as the columnar analytics and inference store. Use `lichess-data columnstore-sync` instead of `duckdb-sync`.
